* Make the UI actually output the articles
* Make a new/edit page. Make the edit page show a delete button.
* Move the db config out into a file (yaml? i kinda like the string way)
* Tests with rack-text-request thingie?